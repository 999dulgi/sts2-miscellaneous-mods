You are going to create a Slay the Spire 2 mod. The top-level folders contain the mods, and each folder contains a mod project. The path to the folder containing the decompiled .dll files required for the mod is "C:\Users\rlawh\sts2Decompile". 

You can build each projects by using "dotnet build"

If you use sensitive commands like `rm -rf`, always confirm before executing them. 

Use Korean but you can use English when there is no equivalent word in Korean.